import InstructionTemplate from "../InstructionTemplate";

export default function InsTargets({}) {
    return (
        <InstructionTemplate title="1. Try to hit all targets in green" imgName="target"/>
    )
}